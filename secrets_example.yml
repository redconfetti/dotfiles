---
# Github Personal Access Token with admin:public_key access
# See https://github.com/settings/tokens
github_personal_access_token: abcdef123456

# Mac User Password (used to run sudo commands)
mac_admin_pass: abcdef12345

# Some machines are locked down without admin access to install applications
# so if you have a separate user with admin rights, you can configure it here
admin_user: john.smith
admin_pass: abcdef12345
